#
#
server {
        listen 80 default_server;
        listen [::]:80 default_server;
        server_name {{ ansible_host }};

        location / {
          include proxy_params;
          proxy_pass http://localhost:5000;
        }
}
